import React from 'react'
import LoginContainer from '../Containers/LoginContainer'

const LoginPage = () => {
    return (
        <div style={{display:'grid',
            placeItems:'center', padding:'40px'
        }}>
           <LoginContainer/> 
        </div>
    )
}

export default LoginPage
